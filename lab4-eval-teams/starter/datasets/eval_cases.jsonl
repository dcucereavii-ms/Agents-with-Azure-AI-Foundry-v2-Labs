{"query": "What is Azure AI Foundry?", "ground_truth": "Azure AI Foundry is a unified platform from Microsoft that enables developers to build and deploy AI-powered applications and agents, providing access to models, tools, and services through a single interface.", "context": "Azure AI Foundry is Microsoft's unified platform for building, deploying, and managing AI applications and agents. It provides access to AI models, manages connections, and supports deploying agents to production environments. Foundry v2 adds improved agent capabilities, better SDK support, and enhanced observability."}
{"query": "How do I create an agent with the Azure AI Projects SDK?", "ground_truth": "Use AIProjectClient to connect to your project, then call client.agents.create_agent() with the model deployment name, agent name, and system instructions. Tools like CodeInterpreter or BingGrounding can be added via ToolSet.", "context": "The azure-ai-projects SDK provides AIProjectClient as the main entry point. Agents are created with create_agent() and can be equipped with tools including CodeInterpreterTool, BingGroundingTool, and custom FunctionTools, all added via a ToolSet."}
{"query": "What is the Model Context Protocol (MCP)?", "ground_truth": "Model Context Protocol is an open standard for enabling LLMs to interact with external tools and data sources through a standardized interface, supporting multiple transport types including stdio and HTTP.", "context": "MCP defines a client-server protocol for exposing tools to LLMs. Any MCP-compatible client (including Azure AI agents) can connect to any MCP server without custom integration code. Common transports are stdio (for local subprocess servers) and HTTP/SSE (for remote servers)."}
{"query": "What evaluators are available in azure-ai-evaluation?", "ground_truth": "azure-ai-evaluation provides evaluators including GroundednessEvaluator, CoherenceEvaluator, RelevanceEvaluator, FluencyEvaluator, and SimilarityEvaluator, each scoring responses from 1-5 using an AI judge.", "context": "Azure AI Evaluation is the official quality-assessment SDK for AI responses. Built-in evaluators include Groundedness, Coherence, Relevance, Fluency, and Similarity. Each uses a judge LLM and returns a score from 1 to 5."}
{"query": "How does OpenTelemetry tracing work with Azure AI agents?", "ground_truth": "Configure OpenTelemetry with configure_azure_monitor() for the exporter and AIInstrumentor().instrument() to auto-trace Azure AI SDK operations. Traces appear in Azure AI Foundry's Tracing tab and Azure Monitor.", "context": "The azure-monitor-opentelemetry package handles the OTLP export to Application Insights. AIInstrumentor patches the azure-ai-projects SDK to emit spans automatically for every agent run, tool call, and message — no manual span creation required."}
{"query": "What is the difference between a thread and a run in the agents SDK?", "ground_truth": "A thread stores the conversation history (messages), while a run represents one agent execution within that thread. Runs process existing messages and produce assistant responses; multiple runs in one thread enable multi-turn conversations.", "context": "Threads persist across runs and maintain message history. Each run triggers the agent to process all messages in the thread and respond. The run lifecycle includes states like queued, in_progress, completed, and failed."}
{"query": "How do I deploy an agent to production with Azure AI Foundry?", "ground_truth": "Promote an evaluated agent by tagging it with metadata (version, eval scores, pass date), exposing its REST endpoint, and sharing the Foundry Playground URL so users can interact with it. Use the agent's metadata field on the create or update call to record promotion data.", "context": "Production promotion in Azure AI Foundry consists of: (1) creating the agent, (2) running an evaluation gate, (3) tagging the agent via metadata when the gate passes, and (4) exposing the agent endpoint or Playground URL for consumers."}
{"query": "What is Bing grounding in Azure AI agents?", "ground_truth": "BingGroundingTool enables agents to perform live web searches via Bing, retrieving up-to-date information to ground responses in current facts. It requires a Bing connection resource configured in the Azure AI Foundry project.", "context": "Bing grounding uses the Bing Search API through a connection configured in Azure AI Foundry. The BingGroundingTool class from azure.ai.projects.models wraps this connection and makes it available to agents during runs."}
