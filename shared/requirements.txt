# Pinned versions for Microsoft Foundry v2 + Microsoft Agent Framework (MAF).
#
# - azure-ai-projects 2.1.x is the stable v2 SDK -- gives us PromptAgentDefinition
#   + create_version (agents land in the NEW Foundry "Agents" page).
# - agent-framework-foundry brings in agent-framework-core + the FoundryAgent
#   wrapper, which lets us orchestrate v2 agents with MAF patterns
#   (SequentialBuilder, ConcurrentBuilder, Handoff, Magentic, ...).
# - aiohttp is required for the async AIProjectClient that FoundryAgent uses
#   under the hood.
#
# Requires Python 3.10+.
azure-ai-projects>=2.1.0,<3.0
agent-framework-foundry>=1.5.0,<2.0
# Orchestration builders (SequentialBuilder, ConcurrentBuilder, Handoff,
# GroupChat, Magentic) ship as a separate pre-release package; an explicit
# version pin lets pip install it without --pre.
agent-framework-orchestrations==1.0.0rc1
aiohttp>=3.9.0
azure-identity>=1.17.1
azure-monitor-opentelemetry>=1.6.4
python-dotenv>=1.0.1
rich>=13.9.4
